export enum Pages {
  // Auth
  SIGN_IN = '/auth/sign-in',

  // Public
  ROOT = '/',
  EVENTS = '/events',

  // Admin
  ADMIN_ROOT = '/admin',
  ADMIN_EVENTS = '/admin/events',
}

export default Pages;
