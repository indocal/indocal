export enum Pages {
  // Auth
  SIGN_IN = '/auth/sign-in',

  // Sections
  ROOT = '/',
  USERS = '/users',
  USERS_ROLES = '/roles',
  USERS_GROUPS = '/groups',
  FORMS = '/forms',
  FORMS_PREVIEW = '/forms/preview',
  FORMS_ENTRIES = '/forms/entries',
  SETTINGS = '/settings',
}

export default Pages;
