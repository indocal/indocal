export enum Pages {
  // Auth
  SIGN_IN = '/auth/sign-in',

  // Sections
  ROOT = '/',
  USERS = '/users',
  USERS_ROLES = '/roles',
  USERS_GROUPS = '/groups',
  FORMS = '/forms',
  SETTINGS = '/settings',
}

export default Pages;
