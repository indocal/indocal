export enum Pages {
  // Auth
  SIGN_IN = '/auth/sign-in',
  FORGOT_PASSWORD = '/auth/forgot-password',

  // Sections
  ROOT = '/',
  SERVICES = '/services',
  SETTINGS = '/settings',
}

export default Pages;
